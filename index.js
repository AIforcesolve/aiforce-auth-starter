/**
 * OAuth2.0 및 JWT 기반 인증 서버 구현
 * TODO: Implement the challenge starter.
 */

function solve(input) {
  // BUG: starter returns wrong result
  return null;
}

if (require.main === module) {
  console.log(JSON.stringify({ ok: false, hint: "Implement solve()" }, null, 2));
}

module.exports = { solve };
